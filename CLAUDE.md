# CLAUDE.md

## Extension Versioning

- Whenever an extension behavior changes (parsing, sorting, request/response handling, filters, or video extraction), bump that extension's `extVersionCode` in its `build.gradle`.
