# AGENTS.md

## Extension Versioning Rule

- If you change extension behavior (for example: episode parsing/sorting, selectors, API calls, or stream extraction), increment that extension's `extVersionCode` in its `build.gradle` as part of the same change.
